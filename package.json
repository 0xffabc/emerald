{
  "name": "emerald",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "test": "rstest"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.7.1",
    "@rstest/core": "^0.8.3",
    "typescript": "^5.9.3"
  }
}
